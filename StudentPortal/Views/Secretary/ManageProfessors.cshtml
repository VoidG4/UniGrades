@model IEnumerable<StudentPortal.Models.Professor>
@{
    Layout = "~/Views/Shared/_SecretaryLayout.cshtml";
    ViewData["Title"] = "Διαχείριση Καθηγητών";
    ViewBag.CurrentPage = "Professors";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold text-dark">Διδακτικό Προσωπικό</h4>
    <a asp-action="CreateProfessor" class="btn btn-primary shadow-sm" style="background-color: #6f42c1; border-color: #6f42c1;">
        <i class="bi bi-person-plus-fill me-2"></i>Νέος Καθηγητής
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="card shadow-sm border-0">
    <div class="card-body p-0">
        <table class="table table-hover align-middle mb-0">
            <thead class="bg-light">
                <tr>
                    <th class="ps-4">AFM</th>
                    <th>Ονοματεπώνυμο</th>
                    <th>Τμήμα</th>
                    <th>Username</th>
                    <th class="text-end pe-4">Ενέργειες</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="ps-4 fw-bold">@item.Afm</td>
                        <td>
                            <div class="d-flex align-items-center">
                                <div class="bg-primary bg-opacity-10 text-primary rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 32px; height: 32px; font-size: 12px; color: #6f42c1 !important; background-color: rgba(111, 66, 193, 0.1) !important;">
                                    @item.Name.Substring(0, 1)@item.Surname.Substring(0, 1)
                                </div>
                                @item.Surname @item.Name
                            </div>
                        </td>
                        <td><span class="badge bg-light text-dark border">@item.Department</span></td>
                        <td class="text-muted small">@item.UsersUsername</td>
                        <td class="text-end pe-4">
                            <form asp-action="DeleteProfessor" asp-route-id="@item.Afm" method="post" onsubmit="return confirm('Διαγραφή καθηγητή;');">
                                <button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>